# 性能调优

目前 LCD 默认进行了最基础的性能优化，即列车状态不变时不绘制 LCD。**暂时没有其它提升性能的选项可用。**

受此影响，列车行驶进度的绘制默认处于关闭状态。如果想要显示进度，可以在 `draw_helper.js` 文件中，将开头的 `showRailProgress` 常量的值改为 `true`。

::: danger 警告
这样做可能导致性能大幅下降。请等待下个版本的改进，使得绘制列车行驶进度的同时不过分降低性能。
:::